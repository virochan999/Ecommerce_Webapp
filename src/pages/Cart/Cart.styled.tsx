import styled from "styled-components"

export const CartWrapper = styled.div`
  margin-top: 1rem;

  & .cart-header {
    display: flex;
    justify-content: space-around;
    align-items: center;
    padding: 0 5rem 0 0;
  }
`
